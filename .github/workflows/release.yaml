name: test-workflow 

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
      with:
        submodules: recursive
    - name: configure
      run: make bt
    - name: make
      run: cd build && make
    - name: Run Test
      run: /home/runner/work/bonobo/build/test/bonobo.test
