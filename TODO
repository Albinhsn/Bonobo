TODO:
  fix for loop speed
  look for general improvements in vm
    remove frameindex just check last frame 
    see what needs to be sent where aka remove VM as a type 
      



git ls-files | grep '\.hs' | xargs wc -l

forsta 
bring start var as local 
store instructions for stop 
after loop run inc expression code 
"return" after condition is True 
Stack: 
  START CODE  
  CON CODE 
   

