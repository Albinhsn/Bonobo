TODO:   
  check operandswidth in bytestring and if it's valid or not  
    - test if vars is over 256?
  better func to remove x items from array
  check if variable already exists in constants and point to that variable in that case
  Rework addOp -> evalAddOp to be one func
  remove LTOp, SubOp 
  implement !! or --
  change from 256 to 66566 or w/e 
  check every warning :)
  change identExp 'ident' token -> String
  addInstruction -> Token, Compiler -> Compiler
  fix 'run'
  better testing for vars in VM




After compiler:
  Test line numbers etc
  fucking test invalid code
  Fix README.md 
  Do aoc examples
  VALIDATION/ERROR HANDLING
    check on semicolon valid statement
    check at eof that last exp is valid
  Fix REPL/interpretor
  https://interpreterbook.com/lost/
  Do efficency testing 
  prebuilts:
    split? 
    read file?

git ls-files | grep '\.hs' | xargs wc -l

NOTES:
  Vars and funcs can be same list/map
  Only return Tokens -> Statements etc 
  
TODO haskell:
  diff types 
  "append"
  NOT A FUCKING DATA EVERYWHERE C:

CheatSheet:
  Conversions 
    https://stackoverflow.com/questions/3232074/what-is-the-best-way-to-convert-string-to-bytestring
  Subarray
    https://stackoverflow.com/questions/5522074/taking-sub-arrays-in-haskell
-- putStrLn (prettyPrint c)
-- line <- Prelude.getLine 
-- if Prelude.null line 
--   then return ()
--   else do
--     putStrLn (prettyPrint (BSU.fromString line))
--     main 

data VM = VM{
    instruct :: !ByteString,
    const:: ![Object],
    global :: ![(int, Object)],
    stack :: ![Object] 
  }
