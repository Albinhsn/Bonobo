ROADMAP FUNCS:
  can't have callParams with same var name as others
  get func with param to compile
  fix vm with params 
  fix scopes a la python

git ls-files | grep '\.hs' | xargs wc -l

-- putStrLn (prettyPrint c)
-- line <- Prelude.getLine 
-- if Prelude.null line 
--   then return ()
--   else do
--     putStrLn (prettyPrint (BSU.fromString line))
--     main 

