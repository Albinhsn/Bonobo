TODO:
  change BP to always be 0 at last frame, inc the ones behind instead 
  fix setLocal and setGlobal 
    use BP from idx of frame + instruct + offset
        


git ls-files | grep '\.hs' | xargs wc -l

-- putStrLn (prettyPrint c)
-- line <- Prelude.getLine 
-- if Prelude.null line 
--   then return ()
--   else do
--     putStrLn (prettyPrint (BSU.fromString line))
--     main 

/* fn print(s){}; fn len(s){}; fn append(a,b){}; */

fn a()
  c = 10 
  fn b()
    d = 2 
    c = c - d; 


