TODO:
  Redo args to be prior to call and eval params and add them when you "run func"
  Fix func with args 
  Fix basepointer for x2 funcs
    store in frames (Int, ByteString)
  Redo global/local into separate ones and gets sent into func 
    Func in func gets locals and put in global, not sent back
  Do massive test suite for things
  Do issue #3 (Prebuilt funcs)
  Do issue #4 (Compiler issues)
  Do issue #5 (Cleanup project after completion)
  Do issue #8 (Look at lost chapter for macros)



git ls-files | grep '\.hs' | xargs wc -l

-- putStrLn (prettyPrint c)
-- line <- Prelude.getLine 
-- if Prelude.null line 
--   then return ()
--   else do
--     putStrLn (prettyPrint (BSU.fromString line))
--     main 

