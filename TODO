ROADMAP:
  Compile funcs without args or bindings 
  Run func without args or bindings
  Compile funcs with local bindings 
  Run func with local bindings 
  Compile funcs with args 
  Run func with args 


git ls-files | grep '\.hs' | xargs wc -l

-- putStrLn (prettyPrint c)
-- line <- Prelude.getLine 
-- if Prelude.null line 
--   then return ()
--   else do
--     putStrLn (prettyPrint (BSU.fromString line))
--     main 

